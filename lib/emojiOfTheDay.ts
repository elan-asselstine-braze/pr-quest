/** Submit a PR to change the emoji! Update this file and open a pull request. */
export const emojiOfTheDay = "🦄";

/** Increment this when the emoji is changed (e.g. after merging a PR). */
export const emojiChangeCount: number = 0;
